# Upstream Merge Audit - 2026-06-08

| File | Local intent | Upstream intent | Resolution | Risks |
| --- | --- | --- | --- | --- |
| `AGENTS.md` | Keep the shared agent entrypoint concise, with Caliber-managed sections and a short operational index. | Add a lean but much more detailed conventions/pitfalls reference. | Kept the local intro, source-of-truth guidance, workflow skills, commands, verification, and Caliber-managed sections; appended the upstream conventions/pitfalls content as the detailed reference block. | Some guidance is intentionally duplicated across the local summary and upstream detail. Future edits should preserve the Caliber-managed sections exactly. |
| `CLAUDE.md` | Keep Claude Code pointed at project status docs, with a compact working-rules header. | Replace the status stub with the current upstream feature/status snapshot, including the new Storico section. | Preserved the local intro and working rules, then kept the upstream architecture, feature, testing, integration, and design sections with the latest status bullet. | The file is now more detailed than the local baseline, but the content is still documentation-only and consistent with the upstream status snapshot. |
| `Draft Release Temp.md` | Preserve the existing release-note bullets. | Add the new Storico per-instrument feature note. | Prepended the upstream Storico "Valore per Strumento" note and kept the existing release-note content. | This is a temporary doc by design; the only risk is future churn if the same note is regenerated elsewhere. |
| `README.md` | Keep the product README aligned with the fork's existing allocation, cashflow, history, and FIRE descriptions. | Add the upstream Allocation redesign, cashflow transfer and long-history updates, per-instrument historical breakdown, and the newer FIRE/goal-investing details. | Merged the feature bullets to include both sides where compatible, preferring the newer upstream behavior where the old wording was obsolete. | The README now contains some overlapping historical wording. Re-running docs generation may reformat a few bullets. |
| `package.json` | Keep the fork's Playwright/e2e and test tooling scripts. | Add Storybook scripts and Storybook-related dev tooling. | Unioned the scripts and devDependencies so both Playwright and Storybook tooling remain available. | If package metadata is regenerated later, npm may reorder keys or refresh the lockfile. |
| `package-lock.json` | Preserve the fork's Playwright-related lock entries. | Preserve the upstream Storybook-related lock entries and the expanded package graph. | Manually merged the root package metadata plus the conflicted `@playwright/test` / `@oxc-resolver` and `modern-tar` / `minipass` regions. | The lockfile was resolved by hand rather than regenerated; a future `npm install` could re-sort or refresh transitive metadata. |
| `__tests__/budgetUtils.test.ts` | Keep the attribution-profile regression coverage and the local `createId` fixture behavior. | Add the broader forecast, insights, alerts, and overall-budget coverage. | Kept the local attribution test and fixture helper, then retained the upstream coverage for forecasting, insights, alerts, and section-order behavior. | Test data now spans both the older monthly defaults and the newer budget-period logic; regressions in shared helpers will surface here first. |
| `lib/utils/budgetUtils.ts` | Preserve the local attribution-aware budget keying and legacy budget auto-init/default helpers. | Add the upstream forecast, insights, alerts, section ordering, and opt-in reconcile behavior. | Merged the attribution filter and key suffix behavior into the upstream utility set, and kept the local auto-init/default helper API alongside the newer amount-based forecast and alert logic. | The module now supports both the legacy helper surface and the newer period-based budget model; future refactors must keep the two call patterns aligned. |
| `lib/services/assetService.ts` | Preserve local investment-quantity updates, household attribution, and Timestamp-safe asset mutations. | Add upstream atomic cash-balance reconciliation for transfer flows. | Kept `updateInvestmentAssetQuantity()` intact and added `updateCashAssetBalancesAtomic()` alongside the existing single-asset cash updater, with transaction updates using `Timestamp.now()` for compatibility. | The combined service now exposes both single-asset and atomic multi-asset update paths; callers must choose the atomic helper for transfer-style multi-update flows. |
| `lib/services/expenseService.ts` | Preserve local household audit entries, investment-operation/cashflow edit behavior, and existing API contracts. | Add upstream transfer support and transfer-aware cashflow updates. | Kept the local investment-linked fields and attribution/audit behavior, added `transferCashAssetId` handling on create/update, preserved transfer sign logic, and kept `Timestamp.now()` for updates. | Recurring/installment paths still only persist the first linked cash asset/update metadata; if a future flow needs transfer metadata there, it will need an explicit extension. |
| `lib/services/pdfDataService.ts` | Preserve the local PDF aggregation flow and cached expense reuse. | Add upstream cashflow filtering semantics, transfer exclusion, and FIRE behavior that ignores filtered expense subsets. | Kept the Italy-timezone month keying and added transfer exclusion in cashflow aggregation; FIRE now always uses annual expense/income services so monthly/yearly PDF filters do not distort FIRE metrics. | PDF FIRE totals now intentionally diverge from the cached expense subset used by cashflow; this matches upstream semantics but changes the previous filtered-expense shortcut. |
| `lib/utils/pdfTimeFilters.ts` | Preserve Firebase Timestamp compatibility and Italy-timezone period extraction. | Keep the newer PDF period filtering semantics. | Kept `toDate()` + `getItalyMonthYear()` for expense filtering while retaining the Italy-based current-year/current-month defaults, so Timestamp-backed expenses remain filterable. | No functional blocker, but any future caller that passes raw JS dates still goes through the `toDate()` normalization path. |
| `app/page.tsx` | Keep the local landing copy that mentions the broader asset set, while preserving the upstream landing refresh. | Refresh the feature list layout and wording on the public landing page. | Kept the local feature description that still mentions pension funds, and kept the upstream two-column landing feature layout. | Low risk. This is presentation-only and the final page now matches the refreshed upstream structure. |
| `app/dashboard/page.tsx` | Preserve the fork's household scope filtering and scoped dashboard computations. | Add upstream dashboard refreshes, token-safe KPI styling, and the new overview charts/summary flow. | Merged the household-scoped overview pipeline with the upstream chart and KPI updates, using `rawOverview` plus a scoped override when household filtering is active. | Medium risk: this page now depends on both the shared overview cache and the scoped asset snapshot cache; regressions would show up as stale or mismatched dashboard KPIs. |
| `app/dashboard/allocation/page.tsx` | Preserve household-scope filtering and the fork's asset ownership view. | Ship the upstream allocation redesign with hero, band, plan, contribution, breakdown, and exposure sections. | Kept the upstream single-page allocation redesign and reintroduced household-scoped asset filtering before allocation comparison, with the scope selector in the header actions. | Medium risk: the page now derives allocation from scoped assets, so any household scope bug will directly affect the displayed rebalance plan. |
| `app/dashboard/assets/page.tsx` | Preserve household scoping and the cash-account edit flow. | Add the upstream asset hero/card redesign and the collapsible fiscal section. | Kept household-scoped metrics and table data, used the upstream collapsible fiscal block, and preserved the cash-account detail/edit dialog with cache invalidation. | Medium risk: the asset table now excludes cash accounts explicitly, so a future `AssetManagementTab` change must keep that contract aligned. |
| `app/dashboard/cashflow/page.tsx` | Preserve cashflow investment-edit permission behavior and household/ownership support. | Add upstream cashflow analysis/budget routing updates and lazy tab loading. | Merged the URL-synced lazy tab shell with the local household compensations tab and the current-year/total-history tabs, while keeping the dividend/assets secondary data loaders. | Medium risk: tab availability now depends on both loaded settings and household scope, so invalid tab params must keep redirecting to a visible tab. |
| `app/dashboard/settings/page.tsx` | Preserve the household settings section and the expanded email preference model. | Add upstream settings tab routing and the newer email toggles. | Kept the household configuration tab and merged the upstream URL-synced settings tab navigation with the semi-annual and weekly budget email flags. | Medium risk: this page has a lot of interdependent state, so any future change to the tab list or email toggles must update the load/save/baseline paths together. |
| `components/assets/AssetDialog.tsx` | Preserve the fork's pension-fund fields, household ownership metadata, and Italian copy. | Keep the upstream asset-dialog validation cleanup and the expanded type set. | Kept the local `pensionfund` fields, ownership profile field, and Italian validation/message copy, while preserving the upstream schema cleanup pattern. | Low-to-medium risk: ticker validation is intentionally conditional and should remain aligned with the asset-type rules. |
| `components/assets/AssetManagementTab.tsx` | Preserve the ownership attribution column and local asset metadata display. | Ship the upstream asset table redesign with the ticker folded into the name cell and dynamic delta columns. | Kept the upstream table layout and dynamic delta `colSpan`, and reinserted the `Proprietà` column for ownership attribution. | Medium risk: the row/col counts now depend on `showDeltas`; future column changes must update the grouped-row `colSpan` in lockstep. |
| `components/assistant/AssistantPageClient.tsx` | Preserve household scope filtering and the local assistant scope label. | Ship the upstream assistant period selector, suggestions banner, memory facts, follow-ups, and preferences popover split. | Kept the upstream assistant shell and re-added the household scope selector plus the scope fields sent with assistant requests. | Medium risk: the scope selector is now separate from the preferences popover, so future layout changes must keep the selected scope in sync with the request payload. |
| `components/cashflow/BudgetTab.tsx` | Preserve household attribution filtering for budget views. | Ship the upstream budget settings, forecast, insights, alerts, and compact opt-in budget list. | Replaced the legacy tab with the upstream budget UI and reintroduced a household attribution filter that maps the visible budget items by ownership profile. | Medium risk: this relies on `BudgetItem` attribution fields already present in saved configs; items without attribution fall back to the unfiltered view. |
| `components/cashflow/ExpenseTrackingTab.tsx` | Preserve the fork's cashflow behavior while keeping edit/delete flows for unified movements. | Ship the upstream unified movement UI, transfer support, filters, and mobile/desktop split. | Kept the upstream unified movement implementation, including transfer support and household scope filtering, by resolving the file to the stage-3 merged version. | Medium risk: this tab now depends on several shared movement helpers and asset reconciliation paths; regressions here would affect cashflow, investment, and transfer flows together. |
| `components/expenses/ExpenseDialog.tsx` | Preserve the fork's investment-operation edit/delete fixes, household attribution, and Italian copy. | Ship the upstream responsive modal, transfer support, and new form structure. | Kept the upstream responsive dialog/drawer implementation, along with the investment and household fields already present in the merged stage-3 version. | Medium risk: the form is larger and now shares more state between create/edit flows; conditional-field resets need to stay aligned with the schema. |
| `components/expenses/ExpenseTable.tsx` | Preserve the fork's deletion reconciliation and attribution column. | Ship the upstream sortable/filtered table redesign and smarter empty-state text. | Kept the upstream sortable table with the transfer-aware delete path and the attribution column in the row layout. | Medium risk: delete flows still invalidate asset caches conditionally; future movement types must update the reconciliation branch too. |
